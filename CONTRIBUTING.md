# コントリビューションガイド

## 現在の段階

本repositoryは、社会シミュレーション「静かなオーケストレーターとしての日本」のMVPレビュー段階にある。2026年から2045年までの年次状態遷移と、入れ子の72時間ストレステストを実行できるローカルWebアプリを収録している。

## 提案の出し方

1. [README](README.md)で中心仮説と反証条件を確認する。
2. [OSINT根拠台帳](evidence.md)で、観測事実・公式評価・設計推論の区分を確認する。
3. [シミュレーション契約](simulation-contract.md)で、時間軸、seed、情報、権限、回線、指標の公平性を確認する。
4. [ADR一覧](docs/adr/README.md)で、二重時間軸、決定論コア、日本不在時の成功条件を確認する。
5. 変更は専用branchからPull Requestとして提案し、目的、変更点、反証可能性への影響、検証結果を書く。

## ローカル検証

Node.js 20以降とnpmを使う。

```powershell
cd app
npm install
npm test
npm run build
npm run test:sites
```

開発プレビューはループバックへ限定する。

```powershell
npm run dev -- --host 127.0.0.1
```

状態遷移を変更する場合は、同じ初期状態と施策列が同じ結果を返すこと、日本だけに追加情報や権限を与えていないこと、能力の改善と集中・監視・依存の悪化を同時に観測できることをテストする。

## 収録しないもの

- secret、個人情報、非公開URL、第三者原典の非公開コピー
- 実在組織の非公開運用情報や作戦情報
- 根拠のない攻撃帰属、特定国・組織の将来行動の断定
- 現実の作戦立案や脆弱箇所の悪用に直結する具体的手順

## 文書の検証

- MarkdownはUTF-8・LFとし、相対リンク切れと末尾空白を残さない。
- 数値、組織名、公式評価には発表主体と取得日を付ける。
- 長文引用を避け、第三者資料は必要最小限の要約・帰属・公式URLで参照する。
- 実装変更後は、環境構築、テスト、seed固定、再現手順をREADMEとテストへ反映する。
