# ロードマップ｜接続が変わる理由を見えるようにする

## 目的

この作品を、施策によって全体指標だけが変わるMVPから、**誰と誰の接続が、なぜ育ち、どの危機結果へ寄与したかを追跡できる社会シミュレーション**へ発展させる。

2045年の成功条件は一貫している。日本の中心性ではなく、日本が中心から退いた後も検証・相互運用・共同所有が残ることを測る。

## 現在地

| 項目 | 現在 | 目標 |
|---|---|---|
| 年次施策 | 5施策ごとに全体指標への効果を区別 | 投資対象の接続と主体によって効果が変わる |
| アクター | 18論理主体を表示 | 能力、利害、制約、情報アクセスを区別する |
| 接続 | 20本の固定関係を表示 | 接続ごとに成熟、信頼、依存、所有、代替経路を持つ |
| 72時間試験 | 全体指標から3結果を算出 | 12ターンの連鎖と、効いた・壊れた接続を説明する |
| 比較 | 2戦略は固定値、提案戦略は現在値 | 全戦略を同じ初期条件・seed・予算で再実行する |
| AIエージェント | 未実装 | 発話と提案だけを担当し、状態変更は決定論コアが検証する |

## 差別化の原則

国名を性格ラベルにしない。差は、公開された役割を抽象化した次の属性で表す。

- `capabilities`: 検証、外交、法執行、インフラ、研究などの機能
- `incentives`: 何を守ると評価されるか
- `constraints`: 法、同盟、国内正統性、機密、予算、時間
- `evidence_access`: 取得できる証拠の種類と遅延
- `decision_rights`: 提案、検証、承認、実行、拒否の権限

中国を非合理、米国を完全情報、日本を常に善意として扱わない。同じ制約型は国をまたいで再利用する。

## 接続状態 v0.2

各接続は最低限、次の状態を持つ。

```text
source / target
purpose
maturity
trust
verificationAgreement
interoperability
coOwnership
dependency
alternateRoutes
evidenceTypes
lastChangedBy
changeReason
```

例:

```text
J2 外交・分析 ↔ C1 外交・発表
目的: 危機用語の翻訳
成熟度: 42 → 51
信頼: 35 → 39
検証合意: 未締結 → 試行中
代替経路: 0 → 1
副作用: 国内反発 +2
72時間への寄与: 誤帰属回避 +4
```

## 実装フェーズ

### R0｜公開可能な基準点

状態: **実装済み・公開前review中**

- 2026〜2045年の決定論エンジン
- 5施策、18主体、20接続、3つの危機結果
- README、ADR、公式URL、権利・安全境界
- ローカルテスト、build、依存監査、デザインQA

完了条件: 現MVPができること・できないことをREADMEとRESULTSで誤解なく再現できる。

### R1｜変化の説明

優先度: **Must**

- 年次アクション選択時に、増減予定の能力・リスクを表示する
- 年を進めた後に、前値、後値、差分、理由を因果レジャーへ記録する
- 72時間結果から、寄与した能力と悪化させたリスクを逆引きする

完了条件: 評価者が1年進めただけで「何が、なぜ変わったか」を説明できる。

### R2｜接続を状態化する

優先度: **Must**

- 固定配列の接続を、接続状態オブジェクトへ移行する
- 施策ごとに1〜3本の投資対象を選ぶ
- 成熟、信頼、依存、共同所有、代替経路を接続単位で更新する
- 線の太さ・色・破線を、経過年数ではなく接続状態から描画する

完了条件: 同じ施策でも投資先を変えると、異なる接続と危機結果が変化する。

### R3｜主体の制約を状態化する

優先度: **Must**

- 18主体に能力、利害、制約、証拠アクセス、決定権を持たせる
- 情報秘匿、国内説明、機関間競合、民間損失を行動制約として扱う
- `portfolio`分類を実際の投資適格性と効果係数に使う

完了条件: 国名を隠しても、役割と制約から行動差を説明できる。

### R4｜72時間の連鎖を実装する

優先度: **Should**

- 6時間×12ターンのイベント列を実装する
- 検証前の攻撃帰属が政策判断を先回りする単一失敗を追う
- 接続の遅延、遮断、誤解、代替経路への切替をログ化する
- 同じseedでは同じ結果を再生する

完了条件: 危機結果について「どの接続が何ターン目に効いた／壊れたか」を再生できる。

### R5｜AIエージェント層を分離追加する

優先度: **Could**

- AIは発話、提案、異論、説明を生成する
- 決定論コアだけが権限、予算、状態遷移を確定する
- 全入出力を記録し、AIなしでも同じ状態列を再生可能にする
- 既定はローカル・オフラインとし、APIキーを要求しない

完了条件: AIの発話が変わっても、ルール違反の状態変更が起きない。

### R6｜反証と2045年試験

優先度: **Must for final study**

- 5つ以上の真因seedで比較する
- 同盟代理、単独仲介、網状調整などを同条件で実行する
- 係数感度と結論反転点を記録する
- 2045年に日本の中央ノードを外して再試験する

完了条件: 仮説の成功条件だけでなく、縮小・棄却条件をRESULTSへ記録できる。

## 公開マイルストーン

| マイルストーン | 公開範囲 | 停止線 |
|---|---|---|
| P0 公開基準点 | 現MVP、限界、ロードマップ、再現手順 | 人間review後にvisibility変更 |
| P1 説明可能版 | R1の因果レジャー | 同一HEADのテスト・デザインQA |
| P2 接続シミュレーター版 | R2〜R4 | seed比較と反証条件のreview |
| P3 研究候補版 | R5〜R6 | 実験結果、倫理、安全、第三者review |

public化は完成宣言ではない。P0を、問いと限界が検証可能な公開基準点として扱う。
