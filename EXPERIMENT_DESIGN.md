# 実験設計スナップショット｜静かなオーケストレーターとしての日本

状態: **retrospective design snapshot / 未実証**

この文書は仮説、比較条件、評価軸、反証条件の正本である。ただし、Git履歴では初回実装・実測と同じcommit系列で追加されたため、過去の実測に対する事前登録証拠ではない。このrevision以後の実行では、実行前に固定する設計入力として扱う。実行後に得た数値、成功宣言、政策効果は記録しない。実測値は `RESULTS.md`、状態遷移と実行方法は `simulation-contract.md` が所有する。

## 問い

日本が命令主体や単一仲裁者になるのではなく、翻訳、検証、可逆化、複線化、共同所有を提供したとき、検証前の攻撃帰属が政策判断を先回りする連鎖を弱められるか。

## 成功仮説

2045年に日本を中央ノードから外しても、検証、相互運用、共同所有と撤退経路が継続するなら、静かなオーケストレーションは成功候補とする。

日本の発言力、仲介回数、中心性の上昇は成功指標にしない。監視の集中、機密漏洩、日本への単一依存、同盟や多国間制度の空洞化が増える場合は失敗として扱う。

## 単一の協調失敗

南西諸島周辺で海上接触、通信障害、港湾・金融へのDDoS、測位異常、真偽不明映像、保険料上昇、同盟要請が重なる。追跡対象は、**検証前の攻撃帰属が政策判断を先回りすること**に固定する。

真因はワールドエンジンだけが持ち、各主体には断片的な観測だけを与える。入力領域は海洋安全保障とサイバー安全保障に固定し、宇宙、エネルギー、情報、外交は両者を結ぶ接続領域とする。

## 比較条件

| ID | 条件 | 検証対象 |
|---|---|---|
| A | ブロック分断 | 誤認と自己強化の連鎖 |
| B | 同盟代理 | 初動速度と対中回線喪失の交換条件 |
| C | 単独仲裁 | 単一障害点化と中立性への不信 |
| D | 網状調整 | 当事国でも多主体の出口を維持できるか |
| E | 調整崩壊 | 省庁対立、異論圧縮、漏洩による失敗 |

全条件で同じ真因seed、物理状態、情報予算、行動回数を使う。Dだけへ真因、追加情報、強い権限を与えない。

## 事前に固定する評価軸

- 誤帰属を訂正するまでの時間
- 協調と民間インフラの継続性
- 不可逆行動へ進むまでの時間と撤退可能性
- 過剰開示、監視集中、単一依存
- 日本を中央ノードから外した後の継続性
- 米中以外の第三者が保持する検証権と決定権

単一の総合点だけで勝者を決めない。指標間のtrade-offと条件別の分布を併記する。

## 反証・縮小条件

- Dだけに有利な情報や権限を与えなければ勝てない。
- 中国を非合理、米国を完全情報、日本を常に善意としたときだけ改善する。
- EがDと同等以上になり、日本型オーケストレーションの組織条件を説明できない。
- 日本の属性を外しても結果が変わらず、汎用的な危機協調プロトコルへ還元できる。
- seedや係数を変えると結論が容易に反転し、再現可能な傾向がない。

該当時は仮説を守るために条件を後付けせず、縮小、棄却、再設計のいずれかを `RESULTS.md` に記録する。

## 設計凍結と変更規律

実験開始時に、この文書のGit commitをdesign revisionとして記録する。実行後の変更は、結果に合わせた後付けと区別するため、新しいrevisionとして理由を記録し、過去の結果を書き換えない。
